name: Build Toraco Key V5.3

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Check project structure
        run: |
          echo "Current directory:"
          pwd
          echo
          echo "Project files:"
          ls -la
          echo
          echo "App directory:"
          ls -la app
          echo
          echo "Gradle files:"
          find . -maxdepth 3 -type f -name "build.gradle.kts" -o -name "settings.gradle.kts" -print

      - name: Set up Java 17
       
